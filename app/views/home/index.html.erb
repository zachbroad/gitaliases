<div class="space-y-8">
  <div class="sm:flex sm:items-center sm:justify-between">
    <div>
      <h2 class="text-3xl font-bold text-gray-900">Git Aliases</h2>
      <p class="mt-2 text-sm text-gray-700">Discover and share useful Git aliases from the community</p>
    </div>
    <% if user_signed_in? %>
      <div class="mt-4 sm:mt-0 flex space-x-3">
        <%= link_to "My Aliases", my_aliases_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        <%= link_to "Add New Alias", new_alias_path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
      </div>
    <% else %>
      <div class="mt-4 sm:mt-0">
        <%= link_to "Sign Up", new_user_registration_path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
      </div>
    <% end %>
  </div>

  <% if @aliases.any? %>
    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      <% @aliases.each do |alias_item| %>
        <div class="bg-white overflow-hidden shadow-sm rounded-lg border border-gray-200 hover:shadow-md transition-shadow duration-200">
          <div class="px-6 py-5">
            <div class="flex items-center justify-between">
              <%= link_to alias_item, class: "text-lg font-semibold text-gray-900 truncate hover:text-indigo-600" do %>
                <%= alias_item.name %>
              <% end %>
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                git
              </span>
            </div>
            
            <p class="mt-3 text-sm text-gray-600 line-clamp-2">
              <%= alias_item.description %>
            </p>
            
            <div class="mt-4">
              <div class="bg-gray-50 rounded-md p-3 border">
                <code class="text-sm font-mono text-gray-800 break-all">
                  <%= alias_item.code %>
                </code>
              </div>
            </div>
            
            <div class="mt-4 flex justify-between items-center">
              <% if alias_item.user %>
                <%= link_to profile_path(alias_item.user.email.split('@').first), class: "text-xs text-gray-500 hover:text-indigo-600" do %>
                  by <%= alias_item.user.email.split('@').first %>
                <% end %>
              <% else %>
                <span class="text-xs text-gray-500">by anonymous</span>
              <% end %>
              
              <div class="flex space-x-2">
                <button type="button" class="inline-flex items-center px-3 py-1.5 border border-gray-300 text-xs font-medium rounded text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                  Copy
                </button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-12">
      <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
      </svg>
      <h3 class="mt-2 text-sm font-medium text-gray-900">No aliases found</h3>
      <p class="mt-1 text-sm text-gray-500">Be the first to share a Git alias with the community.</p>
      <% if user_signed_in? %>
        <div class="mt-6">
          <%= link_to "Add Your First Alias", new_alias_path, class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        </div>
      <% else %>
        <div class="mt-6">
          <%= link_to "Sign Up to Add Aliases", new_user_registration_path, class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
